import { Message } from "discord.js";

export default function nitroEmoteParse(message: Message) {
  const { content } = message;
  /*
    1. Split the message into an array of emotes
        
    */
}
